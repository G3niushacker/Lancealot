import 'package:flutter/material.dart';

class UpComing extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Padding(
          padding: const EdgeInsets.all(8.0),
          child: Text(
              "This is the design Only and first i will make design for this app and after design completion then i will work on functionality Next Will develop When You'll Give me Documentaion"),
        ),
      ),
    );
  }
}

